# Config generated by Ghostty Config

cursor-style-blink = false
custom-shader-animation = always
clipboard-read = allow
clipboard-write = allow
window-decoration = true
background-blur-radius = 30
# theme = tokyonight
theme = Catppuccin Mocha
 
font-size = 21
font-thicken = true
font-family = CaskaydiaCove Nerd Font

mouse-hide-while-typing = true
gtk-titlebar = false
macos-option-as-alt = true


cursor-style=block
shell-integration=zsh
shell-integration-features=no-cursor,sudo,title

# Go to the N window in tmux - Updated for Ghostty 1.2.0+
keybind = super+Digit1=text:\x00\x31
keybind = super+Digit2=text:\x00\x32
keybind = super+Digit3=text:\x00\x33
keybind = super+Digit4=text:\x00\x34
keybind = super+Digit5=text:\x00\x35
keybind = super+Digit6=text:\x00\x36
keybind = super+Digit7=text:\x00\x37
keybind = super+Digit8=text:\x00\x38
keybind = super+Digit9=text:\x00\x39

# New tmux window on Cmd+t in tmux it is Ctrl+Space+c
keybind = super+t=text:\x00\x63
# Switch to a window using tmux-fzf on Cmd+p in tmux it is Ctrl+Space+w
keybind = super+p=text:\x00\x77
# Rename a tmux window on Cmd+r in tmux it is Ctrl+Space+,
keybind = super+r=text:\x00\x2c
# Close tmux pane on Cmd+w in tmux it is Ctrl+Space+x (it is using a custom keybind in tmux to kill the pane)
keybind = super+w=text:\x00\x78
# Go to the previous tmux window on Cmd+h in tmux it is Ctrl+Space+p
keybind = super+h=text:\x00\x70
# Go to te next tmux window on Cmd+l in tmux it is Ctrl+Space+n
keybind = super+l=text:\x00\x6e
# Toggle pane zoom on Cmd+z in tmux it is Ctrl+Space+z
keybind = super+z=text:\x00\x7a

keybind = shift+enter=text:\n

# Mouse
mouse-hide-while-typing = true
# mouse-scroll-multiplier = 1

macos-titlebar-style = transparent


cursor-opacity = 0.4

font-feature = "+calt,+dlig,+liga,+ss01,+ss02,+ss03"

macos-titlebar-style = hidden

clipboard-read = allow
clipboard-write = allow
clipboard-trim-trailing-spaces = true

# Vim-mode keybindings for Ghostty.
#
# Note: We're missing a number of actions to produce a better vim experience.
# This is what is possible today! But we plan on adding more actions to make
# this even better in the future.

# Entry point
keybind = alt+v=activate_key_table:vim

# Key table definition
keybind = vim/

# Line movement
keybind = vim/j=scroll_page_lines:1
keybind = vim/k=scroll_page_lines:-1

# Page movement
keybind = vim/ctrl+d=scroll_page_down
keybind = vim/ctrl+u=scroll_page_up
keybind = vim/ctrl+f=scroll_page_down
keybind = vim/ctrl+b=scroll_page_up
keybind = vim/shift+j=scroll_page_down
keybind = vim/shift+k=scroll_page_up

# Jump to top/bottom
keybind = vim/g>g=scroll_to_top
keybind = vim/shift+g=scroll_to_bottom

# Search (if you want vim-style search entry)
keybind = vim/slash=start_search
keybind = vim/n=navigate_search:next

# Copy mode / selection
# Note we're missing a lot of actions here to make this more full featured.
keybind = vim/v=copy_to_clipboard
keybind = vim/y=copy_to_clipboard

# Command Palette
keybind = vim/shift+semicolon=toggle_command_palette

# Exit
keybind = vim/escape=deactivate_key_table
keybind = vim/q=deactivate_key_table
keybind = vim/i=deactivate_key_table

# Catch unbound keys
keybind = vim/catch_all=ignore
